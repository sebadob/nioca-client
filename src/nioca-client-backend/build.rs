// use std::env;
// use std::process::Command;
//
fn main() {
    //     dotenvy::dotenv().ok();
    //     let dev_mode = env::var("DEV_MODE")
    //         .unwrap_or_else(|_| "false".to_string())
    //         .parse::<bool>()
    //         .expect("Cannot parse DEV_MODE to bool");
    //
    //     // do not pre-compress during local dev - takes too much timetracking between recompilations and is
    //     // annoying
    //     if !dev_mode {
    //         let mut process = Command::new("just")
    //             .arg("compress")
    //             .spawn()
    //             .expect("executing 'just compress'");
    //         process.wait().expect("process wait");
    //     }
    //     // just to actually print the output for debugging
    //     // assert_eq!(1, 2);
}
